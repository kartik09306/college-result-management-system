# Result Management System (Flask + MySQL)

A minimal Result Management System with three roles: `admin`, `faculty`, `student`.

## Requirements

- Python 3.10+
- MySQL 8.x (or MariaDB)

## Setup

1. Create a virtual environment and install dependencies:

```bash
python -m venv venv
# macOS / Linux
source venv/bin/activate
# Windows (PowerShell)
venv\Scripts\Activate.ps1

pip install -r requirements.txt
